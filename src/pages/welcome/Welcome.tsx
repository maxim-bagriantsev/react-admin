import * as React from 'react';
import {Card} from '@mui/material';


const Welcome = () => {

    return (
        <Card sx={{ mt: 8 }}>
           Welcome
        </Card>
    );
};

export default Welcome;
