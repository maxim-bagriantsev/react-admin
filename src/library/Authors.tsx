import * as React from 'react';
import {Card} from '@mui/material';


const Authors = () => {

    return (
        <Card sx={{ mt: 8 }}>
           Authors
        </Card>
    );
};

export default Authors;
